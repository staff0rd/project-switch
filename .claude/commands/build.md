---
description: Build the project via Docker cross-compilation
allowed-tools: Bash
---

Run the build using `assist run build`. This cross-compiles via Docker and produces binaries in `bin/`.

Show the full output to the user so they can see build progress and any errors.
