version: '3.8'

services:
  build:
    build:
      context: .
      dockerfile: Dockerfile
      target: export
    volumes:
      - ./bin:/output
    command: sh -c "cp -r /windows /linux /output/"
    profiles:
      - build

volumes:
  output: